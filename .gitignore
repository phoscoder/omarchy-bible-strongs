# Dev-only (not shipped in the installed plugin)
AGENTS.md

# Editors / IDEs
.idea/
.vscode/
*.swo
*.swp
*~

# Node
node_modules/

# Release tarballs (built locally, never shipped in the repo)
*.tar.gz

# Raw Strong's-tagged KJV source kept locally for scripts/convert-strongs.js
.tmp-*.json
